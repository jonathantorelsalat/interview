import { CustomInterviewDatePipe } from './custom-interview-date.pipe';

describe('CustomInterviewDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomInterviewDatePipe();
    expect(pipe).toBeTruthy();
  });
});
